function Loader({ onNext }) {
  return (
    <main>
      <h1>This is the loader page.</h1>

      <button onClick={onNext}>
        Placeholder Next
      </button>
    </main>
  )
}

export default Loader
